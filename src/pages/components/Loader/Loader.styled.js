import { ClipLoader } from "react-spinners";
import styled from "styled-components";

export const StyledLoader = styled(ClipLoader)`
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
`;
